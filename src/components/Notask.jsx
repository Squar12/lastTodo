export const Notask = () => {
  return (
    <p
      style={{
        fontFamily: "Arial, sans-serif",
        fontSize: "14px",
        color: "#6b7280",
      }}
    >
      No tasks yet. Add one above!
    </p>
  );
};
